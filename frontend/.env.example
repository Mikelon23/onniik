# ONNIIK - FRONTEND ENVIRONMENT VARIABLES
# Copiar este archivo como '.env' o '.env.local' para desarrollo local.
# NUNCA subir credenciales reales ni el archivo '.env' a Git.

# URL base para realizar peticiones a la API del Backend (debe terminar en /api)
VITE_API_URL="http://localhost:5000/api"
